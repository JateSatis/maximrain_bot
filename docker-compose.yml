services:
  maximrain_bot:
    build:
      context: .
      dockerfile: .Dockerfile
    ports:
      - "3000:3000" # Map host port 3000 to container port 3000
    environment:
      - NODE_ENV=development
